# 融合改动记录

## 2026-08-26：阶段 1 — 统一外壳

- 确认用药助手稳定基线：`523cc38`。
- 确认急救助手稳定基线：`8568056`（v0.20）。
- 创建远程备份分支：
  - `codex/backup-primary-stable-20260826`
  - `codex/backup-emergency-v020-20260826`
- 创建隔离整合分支：`codex/clinical-assistant-shell-20260826`。
- 新增“基层临床助手”统一首页与五项底部导航。
- 原用药首页移动到 `#/medication`，其余既有用药路由保持不变。
- 急救入口暂时指向已发布 v0.20，避免复制或破坏急救内容。
- 新增统一数据契约，规定急救流程只能通过 `drug_id` 引用药品。
- 新增盘点、架构和回退记录，以及融合外壳回归检查。

## 2026-08-26：阶段 2 — 药物身份与品规分层

- 明确 `drug_concepts`（通用药物身份）与 `drug_products`（院内具体品规）两层模型。
- 保留既有 `drug-*` 和 `outpatient-*` 品规 ID，禁止急救流程将其当成通用药物 ID。
- 审计 v0.20 的 10 个急救流程、12 个不同药名和用药助手 556 个品规。
- 在独立急救迁移分支 `codex/emergency-drug-id-migration-20260826` 与 Draft PR #3 保存审计结果。

## 2026-08-26：阶段 3 — 首批通用身份注册

- 新增 12 个稳定 `drug-concept-*` 身份，完整覆盖 v0.20 急救药名。
- 建立 6 条已审计品规关联：阿司匹林 3 条、葡萄糖 2 条、头孢曲松 1 条。
- 急救迁移层已把 12 个药名全部绑定到通用 `drug_id`，未改现有页面与临床内容。
- 品规、库存证据和临床核验状态继续独立；通用身份不声明本院供药或当前库存。
- 两个项目的本地回归及 GitHub Actions 检查均通过。
- PWA 缓存从 v58 更新至 v59，以纳入通用身份注册表。

## 2026-08-27：阶段 4 — 按样图完善统一工作台

- 将底部导航正式调整为：首页、用药、急救、常用、我的。
- 统一首页完成核心模块、常用工具和最近使用布局，不再显示“规划中”占位说明。
- 把 v0.20 的 10 个急救流程作为站内导航接入，并通过 12 个 `drug-concept-*` 身份展示关联用药。
- 新增临床计算、常用医嘱、待办和个人设置页面骨架；未经临床审核的公式与可执行医嘱保持禁用。
- 收藏、药品笔记沿用原本机数据；新增待办也仅保存在当前浏览器，不接收患者身份信息。
- 稳定急救站外链继续作为回退，不修改其线上版本。
- 手机端 390px 的 11 个主要路由完成浏览器检查；无控制台错误或横向溢出。
- 原用药助手全量回归、目录审计、离线与 Worker 检查全部通过；PWA 缓存更新至 v60。

## 2026-08-27：阶段 5 — 个人数据本机隔离

- 新增 `user-data-store.js`，为收藏、分组、笔记、文本标记、待办、设置和本地编辑建立用户命名空间。
- 首次加载时把旧稳定版本机数据一次性迁移给当前本机身份，并记录迁移清单和时间。
- 旧数据只允许归属首个本机身份；后续身份不会继承或读取其他用户的旧数据。
- 新建笔记、标记和待办显式写入 `userId`；读取、修改和删除时校验当前用户归属。
- 完整备份升级为 schema v2，包含 `userId` 与存储范围；仍兼容导入 schema v1 旧备份。
- 草稿阶段继续双写旧键作为可回退镜像；关闭 PR 或回到稳定版后仍能读取最新本机数据。
- 真实登录、云同步和服务器逐行权限尚未接入，不宣称已经具备多设备同步。
- PWA 缓存更新至 v61；新增专项隔离、单次迁移和跨用户不覆盖测试。

## 回退方式

- 未合并前：关闭或删除 Draft PR 即可；两个 `main` 和线上站不受影响。
- 合并后如需回退：把用药助手恢复到备份分支对应提交 `523cc38`。
- 急救迁移改动仅存在于独立分支与 Draft PR #3；关闭 PR 即可放弃，稳定基线仍为 `8568056`。
